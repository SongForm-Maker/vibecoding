# 🚀 자동 커밋 사용 가이드

변경사항이 있을 때마다 자동으로 커밋하는 방법입니다.

## 📋 사용 방법

### 방법 1: 일회성 자동 커밋

현재 변경된 파일들을 한 번만 커밋하고 싶을 때:

```bash
# 배치 파일 실행
auto-commit.bat

# 또는 PowerShell에서 직접 실행
powershell -ExecutionPolicy Bypass -File auto-commit.ps1
```

### 방법 2: 파일 감시 모드 (추천! ⭐)

파일이 변경될 때마다 자동으로 커밋하고 싶을 때:

```bash
# 배치 파일 실행 (가장 쉬운 방법)
auto-commit-watch.bat

# 또는 PowerShell에서 직접 실행
powershell -ExecutionPolicy Bypass -File auto-commit.ps1 -Watch
```

감시 모드에서는:
- 5초마다 파일 변경사항을 확인합니다
- 변경이 감지되면 자동으로 커밋합니다
- 종료하려면 `Ctrl+C`를 누르세요

## 📝 커밋 메시지 예시

스크립트가 자동으로 생성하는 한글 커밋 메시지 예시:

- `페이지: 메인 페이지 수정`
- `컴포넌트: Button 컴포넌트 스타일 수정`
- `스타일: 색상 테마 업데이트`
- `설정: 의존성 패키지 추가`
- `라우팅: 앱 메인 수정`
- `페이지 외 2개 카테고리: 총 5개 파일 수정`

## 🎯 파일 카테고리 분류

스크립트가 자동으로 파일을 분류합니다:

| 카테고리 | 포함 경로 |
|---------|----------|
| **페이지** | `src/pages/` |
| **컴포넌트** | `src/components/` |
| **스타일** | `*.css`, `tailwind.config` |
| **설정** | `package.json`, `tsconfig`, `vite.config` |
| **라우팅** | `App.tsx`, `main.tsx` |
| **문서** | `README.md`, `*.md` |

## ⚙️ 고급 설정

### 감시 간격 변경

기본값은 5초입니다. 다른 간격을 원하면:

```powershell
powershell -ExecutionPolicy Bypass -File auto-commit.ps1 -Watch -Interval 10
```

### Git 사용자 정보 확인

커밋 전에 사용자 정보가 설정되어 있는지 확인합니다:

```bash
git config user.name
git config user.email
```

설정되지 않았다면:

```bash
git config user.name "JY"
git config user.email "ji0eon11@naver.com"
```

## 🔍 커밋 확인

커밋이 잘 되었는지 확인:

```bash
git log --oneline -10
```

## 💡 팁

1. **감시 모드 사용 시**: 개발 중에는 `auto-commit-watch.bat`를 실행해두면 편리합니다
2. **수동 커밋**: 특별한 커밋 메시지를 원하면 직접 커밋하세요
3. **커밋 취소**: 실수로 커밋했다면 `git reset HEAD~1`로 취소 가능

## ⚠️ 주의사항

- `.gitignore`에 포함된 파일은 자동 커밋되지 않습니다
- `node_modules`, `dist` 등은 자동으로 제외됩니다
- 커밋 전에 코드가 정상 작동하는지 확인하세요

---

**즐거운 개발 되세요!** 🎵











